

export default function Schedule() {
  return (
    <section className="flex flex-col items-center justify-center w-full min-h-full mt-32 text-[var(--foreground)] transition-opacity duration-1500 relative overflow-hidden">
      <h1 className="mt-4 text-4xl font-bold">Schedule Page</h1>
        <p className="text-xl mt-8">Stay tuned for the schedule of events!</p>
    </section>
  );
}
